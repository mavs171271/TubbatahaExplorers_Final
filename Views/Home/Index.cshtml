<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tubbataha Explorer Marine Species Directory</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/VisualStudio.styles.css" asp-append-version="true" />
    <style>
        /* Reset margins and paddings */
        body, html {
            margin: 0;
            padding: 0;
            overflow: hidden; /* Prevent scrolling */
            height: 100%; /* Full height for body and html */
        }

        /* Style for the video background */
        .video-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; /* Place video behind other content */
            object-fit: cover; /* Ensure video covers the screen */
        }

        /* Style for the content container */
        .content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: white;
            font-family: Arial, sans-serif;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7); /* Add shadow for better visibility */
        }

        /* Title Styles */
        .title-large {
            font-size: 5rem; /* Default font size */
            font-weight: bold;
            margin: 0;
        }

        .title-small {
            font-size: 2rem; /* Default font size */
            font-weight: normal;
            margin: 0.5em 0;
        }

        /* Description Styles */
        .description {
            font-size: 1.2rem; /* Default font size */
            margin: 1em auto;
            width: 80%;
        }

        .navbar {
            z-index: 1000; /* Ensure navbar is above the video */
        }

        /* Class for toggled navbar */
        .navbar-open .video-background {
            top: 50%; /* Move the video down when navbar is opened */
        }

    </style>
</head>
<body>
    <!-- Video Background -->
    <video class="video-background" autoplay loop muted>
        <source src="/css/bgm/bgVideo.mp4" type="video/mp4">
        <source src="tubbataha-video.webm" type="video/webm">
        Your browser does not support the video tag.
    </video>

    <!-- Content Container -->
    <div class="content">
        <h1 class="title-large">Tubbataha Explorer</h1>
        <h2 class="title-small">Marine Species Directory</h2>
        <p class="description">
            Explore the rich marine biodiversity of the Tubbataha Reefs. Our comprehensive directory includes detailed information about various marine species found in this UNESCO World Heritage site.
        </p>
    </div>
</body>
</html>
